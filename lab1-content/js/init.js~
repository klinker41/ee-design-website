// We will define some of the variables we need to use here.

var temp_reading = 'f';                 // f or c
var display_on_box = false;             // turn the display on or off on the box
var notification_text = '5159911493';   // number to text on temperature off limits
var limit_low = '10'                    // in degrees C
var limit_high = '70'                   // in degrees C

// this will hold the current temp as we get it from the database.
var current_temperature = 'Data Unavailable';